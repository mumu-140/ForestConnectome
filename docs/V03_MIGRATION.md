# v0.3 migration

The extraction schema now separates endpoint taxa, study taxa, assertion status, study role, polarity, raw model confidence and calibrated confidence. Transfer decisions now separate orthology tier from predicate-specific transferability.
